# Example configuration for Image Store Service
# Copy to .env and adjust values for your deployment.

# Path where blobs and metadata will be stored. Default: ./data
ISS_DATA=/var/lib/iss

# Optional: override bind host/port when running via uvicorn/gunicorn
# ISS_HOST=0.0.0.0
# ISS_PORT=8000
